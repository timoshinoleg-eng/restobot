# Vite env variables (prefixed with VITE_ are exposed to the client)
# In dev mode, Vite proxies /widget and /admin to localhost backends.
# In production, leave empty to use same-origin requests.
VITE_API_BASE_URL=
